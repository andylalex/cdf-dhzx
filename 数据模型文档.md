# 兑换中心运营后台 — 数据模型文档

> 更新时间：2026-07-23
> 应用文件：`index.html`（单文件应用，Vanilla JS）

---

## 目录

1. [导航结构](#1-导航结构)
2. [奖品仓库模块](#2-奖品仓库模块)
3. [活动管理模块](#3-活动管理模块)
4. [页面运营模块](#4-页面运营模块)
5. [兑换订单模块](#5-兑换订单模块)
6. [兑换报表模块（兑换报表1）](#6-兑换报表模块兑换报表1)
7. [兑换报表2模块](#7-兑换报表2模块)
8. [全局常量与辅助函数](#8-全局常量与辅助函数)

---

## 1. 导航结构

整个后台为一级导航组「兑换管理」，下含 6 个二级菜单：

| 菜单名称 | panel ID | 说明 |
|---------|----------|------|
| 奖品仓库 | `products` | 奖品商品的管理 |
| 活动管理 | `activities` | 兑换/抽奖活动的创建与管理 |
| 页面运营 | `exchangeCenter` | 兑换中心前端页面的奖品上架与配置 |
| 兑换订单 | `orders` | 用户兑换订单与退款管理 |
| 兑换报表 | `reconciliation` | 积分/贝壳/券收入报表 + 活动分摊报表 |
| 兑换报表2 | `reconciliation2` | 收入账单 + 成本账单 |

---

## 2. 奖品仓库模块

### 2.1 数据模型：`allProducts` 数组

| 字段 | 类型 | 说明 | 示例值 |
|------|------|------|--------|
| `id` | Number | 自增ID | `1` |
| `prizeId` | String | 奖品编号 | `'PRIZE-0001'` |
| `name` | String | 奖品名称 | `'雅诗兰黛持妆粉底液 30ml'` |
| `desc` | String | 奖品描述 | `'到店兑换券'` |
| `sku` | String | SKU编码 | `'EX-CDF-001'` |
| `category` | String | 奖品分类 | `'香水化妆品'` / `'电子券'` / `'虚拟权益'` |
| `tag` | String | 标签（同分类） | `'香水化妆品'` |
| `type` | String | 实物/虚拟 | `'实物'` / `'虚拟'` |
| `original` | Number | 奖品价值 | `880` |
| `img` | String | 图片路径 | `'assets/product-1.jpg'` |
| `channel` | String | 适用门店 | `'三亚国际免税城'` / `'海口国际免税城'` / `'博鳌免税店'` / `'三亚凤凰机场店'` / `'海口美兰机场店'` / `'cdf中免海南'` |
| `relatedPrize` | String | 关联奖品名称 | `'夏日狂欢预约抽奖活动'` |
| `stock` | Number | 可用库存（-1表示不限） | `25` / `-1` |
| `totalStock` | Number | 总库存（-1表示不限） | `100` / `-1` |
| `deadlineType` | String | 有效期类型 | `'days'`（按天数） / `'date'`（按日期） |
| `deadlineValue` | Number\|String | 有效期值 | `30` / `'2026-12-31'` / `-1`（不限） |
| `status` | String | 状态 | `'启用'` / `'停用'` |
| `prizeType` | String | 奖品类型 | `'实物礼品'` / `'优惠券-优选单券'` / `'优惠券-券中心'` / `'预约抽奖'` / `'转盘抽奖'` / `'福利值'` |
| `usageScene` | Array\<String\> | 使用场景（已移除，保留在历史数据中） | `['兑换中心']` |
| `exchangeRule` | String | 兑换规则说明 | `'到店出示兑换码即可领取'` |

### 2.2 奖品类型枚举

新增/编辑弹窗中的可选类型（已移除「福利值」「转盘抽奖」从新增选项，但历史数据保留）：

| 奖品类型 | 说明 |
|---------|------|
| 优惠券-优选单券 | 优选单券类优惠券 |
| 优惠券-券中心 | 券中心类优惠券 |
| 实物礼品 | 实物奖品 |
| 预约抽奖 | 预约抽奖类奖品 |

### 2.3 新增弹窗表单字段

| 表单字段 | 元素ID | 类型 | 说明 |
|---------|--------|------|------|
| 奖品名称 | `f_name` | input | 必填 |
| 奖品描述 | `f_desc` | input | 选填 |
| SKU编码 | `f_sku` | input | 必填 |
| 奖品分类 | `f_category` | select | 香水化妆品/电子券/虚拟权益 |
| 奖品类型 | `f_prizeType` | select | 优惠券-优选单券/优惠券-券中心/实物礼品/预约抽奖 |
| 关联奖品 | `f_relatedPrizeInput` + `f_relatedPrize` | 可搜索下拉 | 文本输入搜索 + 隐藏域存储ID |
| 适用门店 | `f_channel` | select | 各免税城/机场店 |
| 奖品图片 | `f_img` | file | 图片上传 |
| 奖品价值 | `f_value` | input(number) | 原始价值 |
| 可用库存 | `f_stock` | input(number) | -1为不限 |
| 总库存 | `f_totalStock` | input(number) | -1为不限 |
| 有效期 | `f_deadlineType` + `f_deadlineValue` | radio + input | 按天数/按日期 |
| 状态 | `f_status` | select | 启用/停用 |
| 兑换规则 | `f_exchangeRule` | textarea | 兑换规则说明 |

### 2.4 筛选器

| 筛选项 | 元素ID | 选项 |
|--------|--------|------|
| 搜索 | `searchPrize` | 按名称搜索 |
| 奖品类型 | `filterPrizeType` | 全部/优惠券-优选单券/优惠券-券中心/实物礼品/预约抽奖 |
| 状态 | `filterStatus` | 全部/启用/停用 |
| 适用门店 | `filterChannel` | 全部/各门店 |

---

## 3. 活动管理模块

### 3.1 数据模型：`allActivities` 数组

| 字段 | 类型 | 说明 | 示例值 |
|------|------|------|--------|
| `id` | Number | 活动ID | `1` |
| `name` | String | 活动名称 | `'夏日狂欢兑换活动'` |
| `timeRange` | Object | 活动时间范围 | `{ start: '2026-07-19 10:00', end: '2026-07-25 23:59' }` |
| `createdAt` | String | 创建时间 | `'2026-07-18 14:30'` |
| `createdBy` | String | 创建人 | `'管理员'` / `'运营专员'` |
| `status` | String | 活动状态 | `'active'`（进行中） / `'terminated'`（已终止） |
| `rule` | String | 活动规则 | `'每人每场限兑2件'` |
| `channel` | Array\<String\> | 投放渠道 | `['APP', '微信小程序']` |
| `payType` | String | 支付方式 | `'积分'` / `'贝壳'` |
| `costShare` | Object | 成本分摊配置 | 见下方分摊模型 |
| `revenueShare` | Object | 收入分成配置 | 见下方分成模型 |
| `memberDiscount` | Object | 会员折扣 | `{ normal: 100, silver: 95, gold: 90, platinum: 85, diamond: 80 }` |
| `accountType` | String | 记账方式 | `'主营业务'` / `'非主营业务'` |
| `budgetLimit` | Number | 预算上限 | `500000` |
| `actualCost` | Number | 实际成本 | `149600` |
| `actualRevenue` | Number | 实际收入 | `149600` |
| `items` | Array\<Object\> | 奖品配置列表 | 见下方奖品配置模型 |

### 3.2 分摊/分成配置模型

`costShare`（成本分摊）和 `revenueShare`（收入分成）共享同一结构：

| type 类型 | 附加字段 | 说明 |
|-----------|---------|------|
| `'平台全额'` | 无 | 平台承担100% |
| `'门店全额'` | 无 | 门店承担100% |
| `'品牌全额'` | 无 | 品牌方承担100% |
| `'混合分摊'`（成本）/`'混合分成'`（收入） | `brand`（Number, 品牌百分比）、`store`（Number, 门店百分比）、`tax`（Number, 税费百分比） | 按比例分摊，平台 = 100 - brand - store |

### 3.3 奖品配置模型（`items` 数组）

| 字段 | 类型 | 说明 | 示例值 |
|------|------|------|--------|
| `name` | String | 奖品名称 | `'雅诗兰黛粉底液 30ml'` |
| `type` | String | 奖品类型 | `'实物礼品'` / `'优惠券-优选单券'` / `'优惠券-券中心'` |
| `price` | Number | 兑换价格（分值） | `8800` |
| `stock` | Number | 可用库存（-1为不限，显示为「不限」） | `50` / `-1` |
| `exchanged` | Number | 已兑换数量 | `12` |
| `monthLimit` | Number | 月限（-1为不限） | `-1` |
| `dayLimit` | Number | 日限（-1为不限） | `-1` |
| `tags` | Array\<String\> | 标签 | `['限时', '会员专属']` |

### 3.4 会员折扣模型

| 字段 | 说明 | 默认折扣 |
|------|------|---------|
| `normal` | 普通会员 | `100`（无折扣） |
| `silver` | 银卡会员 | `95`（95折） |
| `gold` | 金卡会员 | `90`（9折） |
| `platinum` | 铂金VIP | `85`（85折） |
| `diamond` | 钻石VIP | `80`（8折） |

### 3.5 活动标签

`activityTags` 数组，可自定义标签，应用于活动奖品配置中的标签选择器。

---

## 4. 页面运营模块

### 4.1 模块结构

页面运营模块（`panel-exchangeCenter`）包含 3 个 Tab：

| Tab 名称 | Tab ID | 说明 |
|---------|--------|------|
| 积分奖品 | `centerTabPrizes` | 积分兑换的奖品上架管理 |
| 贝壳奖品 | `centerTabShells` | 贝壳兑换的奖品上架管理 |
| 页面配置 | `centerTabConfig` | 兑换中心页面配置 |

### 4.2 数据来源

页面运营的奖品数据来源于 `allProducts`，通过筛选 `payType`（积分/贝壳）区分。上架状态、排序、标签等通过前端状态管理。

### 4.3 筛选器

| 筛选项 | 元素ID | 说明 |
|--------|--------|------|
| 搜索 | `searchCenterPrize` | 按名称搜索 |
| 活动 | `filterCenterSource` | 按来源活动筛选 |
| 上架状态 | `filterCenterVisibility` | 已上架/全部/已下架 |
| 标签 | `filterCenterTag` | 按标签筛选 |
| 日期范围 | `filterCenterDateStart` / `filterCenterDateEnd` | 上架日期范围 |

---

## 5. 兑换订单模块

### 5.1 数据模型：`allOrders` 数组

| 字段 | 类型 | 说明 | 示例值 |
|------|------|------|--------|
| `orderId` | String | 订单号 | `'EX20260721001'` |
| `prizeId` | String | 奖品ID | `'PRIZE-0001'` |
| `prizeName` | String | 奖品名称 | `'雅诗兰黛持妆粉底液 30ml'` |
| `prizeImg` | String | 奖品图片 | `''` |
| `prizeType` | String | 奖品类型 | `'实物礼品'` / `'优惠券-优选单券'` / `'预约抽奖'` / `'转盘抽奖'` |
| `quantity` | Number | 数量 | `1` |
| `relatedPrize` | String | 关联奖品名称 | `'兰蔻小黑瓶精华肌底液 20ml'` |
| `relatedPrizeId` | String | 关联奖品ID | `'PRIZE-0002'` |
| `sourceType` | String | 来源类型 | `'兑换中心'` / `'预约抽奖'` / `'转盘抽奖'` |
| `sourceActivityName` | String | 来源活动名称 | `'夏日狂欢兑换活动'` |
| `sourceActivityId` | String | 来源活动ID | `'ACT-001'` |
| `userName` | String | 用户姓名 | `'王梦琪'` |
| `userId` | String | 用户ID | `'UID-10001'` |
| `userPhone` | String | 用户手机号（脱敏） | `'138****8888'` |
| `userAddress` | String | 收货地址 | `'海南省三亚市...'` |
| `memberLevel` | String | 会员等级 | `'普通会员'` / `'银卡会员'` / `'金卡会员'` / `'铂金VIP'` / `'钻石VIP'` |
| `payType` | String | 支付方式 | `'积分'` / `'贝壳'` |
| `payPoints` | Number | 应付分值 | `8800` |
| `actualPayPoints` | Number | 实付分值（计算字段） | `7920`（按会员折扣计算） |
| `costAmount` | Number | 成本金额（计算字段） | `880`（payPoints * 0.1） |
| `status` | String | 订单状态 | `'待支付'` / `'待发货'` / `'待收货/待使用'` / `'已完成'` / `'已取消'` / `'退款中'` |
| `time` | String | 下单时间 | `'2026-07-21 10:30'` |
| `completeTime` | String | 完成时间 | `'2026-07-22 14:00'` |
| `expressNo` | String | 物流单号 | `'SF1234567890'` |
| `expressCompany` | String | 物流公司 | `'顺丰速运'` / `'圆通速递'` |
| `accountType` | String | 记账方式 | `'主营业务'` / `'非主营业务'` |
| `usageScene` | String | 使用场景 | `'兑换中心'` / `'任务'` / `'签到'` / `'抽奖'` |

### 5.2 计算字段说明

`actualPayPoints` 和 `costAmount` 在 `_initOrderComputedFields` 函数中延迟计算：

- `actualPayPoints = payPoints * memberDiscount[level] / 100`
- `costAmount = payPoints * 0.1`

### 5.3 退款数据模型：`allRefunds` 数组

| 字段 | 类型 | 说明 | 示例值 |
|------|------|------|--------|
| `refundId` | String | 退款单号 | `'RF20260722001'` |
| `orderId` | String | 原订单号 | `'EX20260711007'` |
| `prizeId` | String | 奖品ID | `'PRIZE-0004'` |
| `prizeName` | String | 奖品名称 | `'资生堂红腰子精华 50ml'` |
| `prizeImg` | String | 奖品图片 | `''` |
| `prizeType` | String | 奖品类型 | `'实物礼品'` |
| `quantity` | Number | 数量 | `1` |
| `relatedPrize` | String | 关联奖品 | `''` |
| `relatedPrizeId` | String | 关联奖品ID | `''` |
| `sourceType` | String | 来源类型 | `'兑换中心'` |
| `sourceActivityName` | String | 来源活动名称 | `'夏日狂欢兑换活动'` |
| `sourceActivityId` | String | 来源活动ID | `'ACT-001'` |
| `userName` | String | 用户姓名 | `'李思远'` |
| `userId` | String | 用户ID | `'UID-10015'` |
| `userPhone` | String | 用户手机号 | `'151****6677'` |
| `userAddress` | String | 收货地址 | `'海南省海口市...'` |
| `memberLevel` | String | 会员等级 | `'铂金VIP'` |
| `payType` | String | 支付方式 | `'积分'` |
| `payPoints` | Number | 退款分值 | `15000` |
| `actualPayPoints` | Number | 实退分值（计算字段） | `12750` |
| `costAmount` | Number | 成本金额（计算字段） | `1500` |
| `reason` | String | 退款原因 | `'到期未使用自动退款'` |
| `status` | String | 退款状态 | `'退款完成'` / `'待审核'` |
| `time` | String | 退款时间 | `'2026-07-22 00:05'` |
| `logs` | Array\<Object\> | 退款日志 | `[{ time, operator, action, content }]` |

### 5.4 退款日志模型

| 字段 | 类型 | 说明 |
|------|------|------|
| `time` | String | 操作时间 | `'2026-07-22 00:05:00'` |
| `operator` | String | 操作人 | `'系统'` / 用户名 |
| `action` | String | 操作动作 | `'自动退款'` / `'申请退款'` / `'退款完成'` |
| `content` | String | 操作内容 | `'奖品到期未使用，系统自动发起退款'` |

### 5.5 订单筛选器

| 筛选项 | 元素ID | 选项 |
|--------|--------|------|
| 搜索 | `searchOrder` | 按订单号搜索 |
| 状态 | `filterOrderStatus` | 全部/待支付/待发货/待收货/已完成/已取消/退款中 |
| 奖品类型 | `filterOrderPrizeType` | 全部/实物礼品/优惠券-优选单券/优惠券-券中心/预约抽奖/转盘抽奖 |
| 来源类型 | `filterOrderSource` | 全部/兑换中心/预约抽奖/转盘抽奖 |
| 记账方式 | `filterOrderAccountType` | 全部/主营业务/非主营业务 |
| 使用场景 | `filterOrderUsageScene` | 全部/兑换中心/任务/签到/抽奖 |
| 日期范围 | `filterOrderDateStart` / `filterOrderDateEnd` | 下单日期范围 |

### 5.6 物流按钮显示规则

- 仅 `sourceType === '兑换中心'` 且 `status !== '已取消'` 的订单显示物流按钮
- 其他来源（预约抽奖、转盘抽奖）或已取消状态的订单不显示物流按钮

---

## 6. 兑换报表模块（兑换报表1）

### 6.1 模块结构

包含 4 个 Tab：

| Tab 名称 | Tab ID | 说明 |
|---------|--------|------|
| 积分收入报表 | `reconTabPoints` | 积分消耗统计 |
| 贝壳收入报表 | `reconTabShells` | 贝壳消耗统计 |
| 券收入报表 | `reconTabCoupon` | 优惠券消耗统计 |
| 活动分摊报表 | `reconTabShare` | 活动成本分摊统计 |

### 6.2 积分收入报表（活动维度）：`pointsReconData`

| 字段 | 类型 | 说明 |
|------|------|------|
| `date` | String | 日期 | `'2026-07-22'` |
| `activity` | String | 活动名称 | `'夏日狂欢兑换活动'` |
| `payType` | String | 支付方式 | `'积分'` / `'贝壳'` |
| `memberLevel` | String | 会员等级 | `'金卡会员'` |
| `consume` | Number | 消耗量 | `8800` |

### 6.3 积分收入报表（奖品维度）：`pointsPrizeReconData`

| 字段 | 类型 | 说明 |
|------|------|------|
| `prizeName` | String | 奖品名称 |
| `prizeType` | String | 奖品类型 |
| `activity` | String | 活动名称 |
| `payType` | String | 支付方式 |
| `count` | Number | 兑换笔数 |
| `consume` | Number | 消耗总量 |

### 6.4 贝壳收入报表（活动维度）：`shellsReconData`

| 字段 | 类型 | 说明 |
|------|------|------|
| `date` | String | 日期 |
| `activity` | String | 活动名称 |
| `payType` | String | 支付方式（固定为`'贝壳'`） |
| `consume` | Number | 消耗量 |

### 6.5 贝壳收入报表（奖品维度）：`shellsPrizeReconData`

| 字段 | 类型 | 说明 |
|------|------|------|
| `prizeName` | String | 奖品名称 |
| `prizeType` | String | 奖品类型 |
| `activity` | String | 活动名称 |
| `payType` | String | 支付方式 |
| `count` | Number | 兑换笔数 |
| `consume` | Number | 消耗总量 |

### 6.6 券收入报表（活动维度）：`couponReconData`

| 字段 | 类型 | 说明 |
|------|------|------|
| `date` | String | 日期 |
| `activity` | String | 活动名称 |
| `payType` | String | 支付方式 |
| `count` | Number | 兑换笔数 |
| `consume` | Number | 消耗总量 |

### 6.7 券收入报表（奖品维度）：`couponPrizeReconData`

| 字段 | 类型 | 说明 |
|------|------|------|
| `prizeName` | String | 奖品名称 |
| `prizeType` | String | 奖品类型 |
| `activity` | String | 活动名称 |
| `payType` | String | 支付方式 |
| `count` | Number | 兑换笔数 |
| `consume` | Number | 消耗总量 |

### 6.8 活动分摊报表

数据来源：直接遍历 `allActivities` 数组，使用以下字段进行分摊计算：

| 数据来源 | 字段 | 说明 |
|---------|------|------|
| `allActivities` | `actualCost` | 成本消耗总量 |
| `allActivities` | `actualRevenue` | 收入总量 |
| `allActivities` | `costShare` | 成本分摊配置 |
| `allActivities` | `revenueShare` | 收入分成配置 |

分摊计算逻辑：

- **平台全额**：platform = consume
- **门店全额**：store = consume
- **品牌全额**：brand = consume
- **混合分摊**：brand = consume * brand%，store = consume * store%，platform = consume - brand - store

统计卡片：

| 指标 | 说明 |
|------|------|
| 积分消耗总量 | 所有活动积分 actualCost 之和 |
| 贝壳消耗总量 | 所有活动贝壳 actualCost 之和 |
| 积分平台承担 | 平台分摊积分之和 |
| 贝壳平台承担 | 平台分摊贝壳之和 |
| 积分品牌方承担 | 品牌分摊积分之和 |
| 贝壳品牌方承担 | 品牌分摊贝壳之和 |
| 积分门店承担 | 门店分摊积分之和 |
| 贝壳门店承担 | 门店分摊贝壳之和 |

---

## 7. 兑换报表2模块

### 7.1 模块结构

一级 Tab（Segment Control 样式）：

| 一级Tab | ID | 说明 |
|---------|-----|------|
| 收入账单 | `bill2Income` | 收入维度统计 |
| 成本账单 | `bill2Expense` | 成本维度统计 |

每个一级 Tab 下有 3 个二级 Tab：

| 二级Tab | 收入账单 | 成本账单 |
|---------|---------|---------|
| 月汇总 | `income-month` | `expense-month` |
| 日汇总 | `income-day` | `expense-day` |
| 明细 | `income-detail` | `expense-detail` |

### 7.2 收入账单数据模型：`bill2IncomeData`

| 字段 | 类型 | 说明 | 示例值 |
|------|------|------|--------|
| `orderId` | String | 订单号 | `'EX20260722001'` |
| `prizeName` | String | 奖品名称 | `'雅诗兰黛持妆粉底液 30ml'` |
| `prizeType` | String | 奖品类型 | `'实物礼品'` |
| `activity` | String | 来源活动 | `'夏日狂欢兑换活动'` |
| `payType` | String | 支付方式 | `'积分'` / `'贝壳'` |
| `amount` | Number | 应收分值 | `8800` |
| `actualAmount` | Number | 实收分值 | `8800` |
| `userPhone` | String | 用户手机号 | `'138****8888'` |
| `time` | String | 兑换时间 | `'2026-07-22 10:30'` |
| `status` | String | 状态 | `'已完成'` / `'退款中'` |

### 7.3 成本账单数据模型：`bill2ExpenseData`

| 字段 | 类型 | 说明 | 示例值 |
|------|------|------|--------|
| `orderId` | String | 订单号 | `'EX20260722001'` |
| `prizeName` | String | 奖品名称 | `'雅诗兰黛持妆粉底液 30ml'` |
| `prizeType` | String | 奖品类型 | `'实物礼品'` |
| `scene` | String | 使用场景 | `'兑换中心'` / `'签到'` / `'任务'` / `'转盘抽奖'` / `'预约抽奖'` |
| `bizType` | String | 业务类型 | `'奖品成本'` / `'券成本'` / `'平台服务费'` / `'福利成本'` |
| `cost` | Number | 总成本 | `4500` |
| `pointsCost` | Number | 积分成本（cost * 80%） | `3600` |
| `shellsCost` | Number | 贝壳成本（cost * 20%） | `900` |
| `time` | String | 时间 | `'2026-07-22 10:30'` |
| `payType` | String | 支付方式 | `'积分'` / `'贝壳'` |
| `amount` | Number | 应收分值 | `4500` |
| `actualAmount` | Number | 实收分值 | `4500` |
| `status` | String | 状态 | `'已完成'` |

### 7.4 收入账单 — 月汇总/日汇总表头

| 列名 | 说明 |
|------|------|
| 月份 / 日期 | 统计维度 |
| 奖品类型 | 按奖品类型聚合 |
| 积分收入 | 积分正向收入 |
| 积分退款 | 积分逆向退款 |
| 积分净收入 | 积分收入 - 积分退款 |
| 贝壳收入 | 贝壳正向收入 |
| 贝壳退款 | 贝壳逆向退款 |
| 贝壳净收入 | 贝壳收入 - 贝壳退款 |
| 平台分成（积分/贝壳） | 平台分成金额 |
| 品牌分成（积分/贝壳） | 品牌方分成金额 |
| 门店分成（积分/贝壳） | 门店分成金额 |
| 操作 | 下载账单 / 分成明细按钮 |

### 7.5 收入账单 — 收入明细表头

| 列名 | 说明 |
|------|------|
| 订单号 | 订单编号 |
| 奖品名称 | 奖品名称 |
| 奖品类型 | 奖品类型 |
| 支付方式 | 积分/贝壳 |
| 应收分值 | 应收金额 |
| 实收分值 | 实收金额 |
| 平台分成 | 平台分成金额（百分比） |
| 品牌分成 | 品牌方分成金额（百分比），含商户名称 |
| 门店分成 | 门店分成金额（百分比），含门店名称 |
| 兑换时间 | 兑换时间 |
| 状态 | 已完成/已退款 |

### 7.6 成本账单 — 月汇总/日汇总表头

| 列名 | 说明 |
|------|------|
| 月份 / 日期 | 统计维度 |
| 使用场景 | 按使用场景聚合 |
| 积分成本 | 积分成本小计 |
| 贝壳成本 | 贝壳成本小计 |
| 平台分摊（积分/贝壳） | 平台分摊金额 |
| 品牌分摊（积分/贝壳） | 品牌方分摊金额 |
| 门店分摊（积分/贝壳） | 门店分摊金额 |
| 操作 | 下载账单 / 分摊明细按钮 |

### 7.7 成本账单 — 成本明细表头

| 列名 | 说明 |
|------|------|
| 订单号 | 订单编号 |
| 奖品名称 | 奖品名称 |
| 奖品类型 | 奖品类型 |
| 使用场景 | 使用场景 |
| 支付方式 | 积分/贝壳 |
| 应收分值 | 应收金额 |
| 实收分值 | 实收金额 |
| 平台分摊 | 平台分摊金额（百分比） |
| 品牌分摊 | 品牌方分摊金额（百分比），含商户名称 |
| 门店分摊 | 门店分摊金额（百分比），含门店名称 |
| 兑换时间 | 兑换时间 |
| 状态 | 已完成 |

### 7.8 分成/分摊计算函数：`calcBill2Share(amount, activityName)`

根据活动名称查找 `allActivities` 中的 `costShare` 配置，计算分成/分摊金额：

| 分摊类型 | platform | brand | store | platPct | brandPct | storePct |
|---------|----------|-------|-------|---------|----------|---------|
| 平台全额 | amount | 0 | 0 | 100% | 0% | 0% |
| 门店全额 | 0 | 0 | amount | 0% | 0% | 100% |
| 品牌全额 | 0 | amount | 0 | 0% | 100% | 0% |
| 混合分摊 | amount - brand - store | amount * brand% | amount * store% | (100-brand-store)% | brand% | store% |

### 7.9 明细弹窗

| 属性 | 说明 |
|------|------|
| 弹窗ID | `bill2DetailModal` |
| Tab 1 | 按门店（默认选中） |
| Tab 2 | 商户名称 |

**按门店列**：门店名称 / 积分收入(或积分成本) / 贝壳收入(或贝壳成本)

**商户名称列**：商户名称 / 积分收入(或积分成本) / 贝壳收入(或贝壳成本)

- 收入账单弹窗表头：积分收入 / 贝壳收入
- 成本账单弹窗表头：积分成本 / 贝壳成本

### 7.10 翻页功能

| 配置项 | 值 |
|--------|-----|
| 每页条数 | `BILL2_PAGE_SIZE = 10` |
| 状态对象 | `bill2PageState` |
| 翻页键 | `income-month` / `income-day` / `income-detail` / `expense-month` / `expense-day` / `expense-detail` |
| 总数键 | `{key}-total` |
| 切换Tab时 | 重置为第1页 |

---

## 8. 全局常量与辅助函数

### 8.1 商户名称数组：`MERCHANT_NAMES`

```javascript
['星巴克咖啡店', '喜茶饮品店', '海底捞餐饮', '万达影院', '三亚度假酒店',
 '海口亚特兰蒂斯酒店', '中免海南商城', '悦诗风吟美妆店', '肯德基快餐', '免税城spa会所']
```

`getMerchantName(str)`：基于字符串 charCode 求和取模，稳定映射到商户名称。

### 8.2 门店名称数组：`STORE_NAMES`

```javascript
['三亚国际免税城', '海口国际免税城', '博鳌免税店', '三亚凤凰机场店', '海口美兰机场店']
```

`getStoreName(str)`：基于字符串 charCode 求和取模，稳定映射到门店名称。

### 8.3 适用门店枚举

| 门店名称 | 说明 |
|---------|------|
| 三亚国际免税城 | 三亚海棠湾 |
| 海口国际免税城 | 海口西海岸 |
| 博鳌免税店 | 琼海博鳌 |
| 三亚凤凰机场店 | 三亚凤凰国际机场 |
| 海口美兰机场店 | 海口美兰国际机场 |
| cdf中免海南 | 线上通用 |

### 8.4 会员等级枚举

| 等级 | 标识 | 默认折扣 |
|------|------|---------|
| 普通会员 | `normal` | 100% |
| 银卡会员 | `silver` | 95% |
| 金卡会员 | `gold` | 90% |
| 铂金VIP | `platinum` | 85% |
| 钻石VIP | `diamond` | 80% |

### 8.5 订单状态枚举

| 状态 | 说明 | 物流按钮 |
|------|------|---------|
| 待支付 | 用户未支付 | 无 |
| 待发货 | 已支付待发货 | 有（仅兑换中心） |
| 待收货/待使用 | 已发货待收货 | 有（仅兑换中心） |
| 已完成 | 订单完成 | 有（仅兑换中心） |
| 已取消 | 用户取消 | 无 |
| 退款中 | 退款流程中 | 有（仅兑换中心） |

### 8.6 奖品类型完整枚举

| 奖品类型 | 使用场景 | 说明 |
|---------|---------|------|
| 实物礼品 | 兑换中心 | 实物奖品，需物流 |
| 优惠券-优选单券 | 兑换中心、签到 | 优选单券 |
| 优惠券-券中心 | 兑换中心、签到 | 券中心券 |
| 预约抽奖 | 兑换中心、签到 | 预约抽奖权益 |
| 转盘抽奖 | 转盘抽奖 | 转盘抽奖机会 |
| 福利值 | 签到、任务 | 福利值（历史数据保留） |

### 8.7 使用场景枚举

| 使用场景 | 说明 |
|---------|------|
| 兑换中心 | 主兑换场景 |
| 任务 | 任务奖励 |
| 签到 | 签到奖励 |
| 抽奖 | 抽奖场景（预约抽奖/转盘抽奖） |

### 8.8 记账方式枚举

| 记账方式 | 说明 |
|---------|------|
| 主营业务 | 核心兑换业务 |
| 非主营业务 | 抽奖等非核心业务 |

### 8.9 成本业务类型枚举

| 业务类型 | 说明 |
|---------|------|
| 奖品成本 | 实物礼品成本 |
| 券成本 | 优惠券成本 |
| 平台服务费 | 平台服务费用 |
| 福利成本 | 福利值成本 |

### 8.10 分摊/分成类型枚举

| 类型 | 适用 | 说明 |
|------|------|------|
| 平台全额 | 成本分摊/收入分成 | 平台承担100% |
| 门店全额 | 成本分摊/收入分成 | 门店承担100% |
| 品牌全额 | 成本分摊/收入分成 | 品牌方承担100% |
| 混合分摊 | 成本分摊 | 按比例分摊（brand% + store% + 平台剩余） |
| 混合分成 | 收入分成 | 按比例分成（brand% + store% + 平台剩余） |
